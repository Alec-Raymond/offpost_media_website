ADMIN_PASSCODE=<bcrypt hash from: npm run hash-passcode -- "your-passcode">
SESSION_SECRET=<random string, e.g.: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">
NODE_ENV=development
